FROM node:20-bookworm-slim
WORKDIR /app

# Copy package files and install dependencies
COPY package*.json ./
RUN npm ci --ignore-scripts && npm install -D @tailwindcss/postcss && npm cache clean --force

# Copy source code
COPY . .

EXPOSE 5173

# Start development server
CMD ["npm", "run", "dev", "--", "--host"]
